services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: ferron-proxy-backend-dev
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped
