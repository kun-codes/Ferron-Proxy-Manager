PUBLIC_AUTH_SIGNUP_DISABLED=false

# Backend URL for server-side API calls (used in hooks.server.ts)
BACKEND_URL=http://localhost:8000

# Backend URL for client-side API calls (used in apiClient.ts)
# This should be accessible from the browser
PUBLIC_BACKEND_URL=http://localhost:8000
